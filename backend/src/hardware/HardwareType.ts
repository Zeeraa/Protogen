/**
 * Enum representing different hardware types.
 */
export enum HardwareType {
  /**
   * The standard Raspberry Pi 4 based implementation.
   */
  STANDARD = "STANDARD",
  /**
   * Emulated implementation for development purposes.
   */
  EMULATED = "EMULATED",
}
